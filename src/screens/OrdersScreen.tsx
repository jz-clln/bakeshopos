// File: app/src/screens/OrdersScreen.tsx
//
// Placeholder — built out in Phase 1E (Orders + order state machine).

import { AppShell } from '../components/layout/AppShell';

export function OrdersScreen() {
  return (
    <AppShell title="Orders">
      <div className="pt-4">
        <p className="text-gray-500">Orders will show up here in Phase 1E.</p>
      </div>
    </AppShell>
  );
}